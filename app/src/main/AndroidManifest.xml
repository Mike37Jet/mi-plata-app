<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No se declara android.permission.INTERNET, y es deliberado.

      La app es offline-first y el backup usa el Storage Access Framework, no
      los SDK de Drive/OneDrive (ver docs/adr/0004). Que el permiso no esté aquí
      convierte "tus datos no salen del dispositivo" en algo verificable por
      cualquiera, no en una promesa de la ficha de Play Store.
    -->

    <application
        android:name=".MiPlataApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiPlata.Starting">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MiPlata.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
